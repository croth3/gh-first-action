name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeing
        run: echo "eat it"
      - name: print goodbye
        run: echo "eat it again"
